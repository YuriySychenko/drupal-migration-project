{#
/**
 * @file
 * Default theme implementation to display a single page.
 *
 * The doctype, html, head and body tags are not in this template. Instead they
 * can be found in the html.html.twig template in this directory.
 *
 * Available variables:
 *
 * General utility variables:
 * - base_path: The base URL path of the Drupal installation. Will usually be
 *   "/" unless you have installed Drupal in a sub-directory.
 * - is_front: A flag indicating if the current page is the front page.
 * - logged_in: A flag indicating if the user is registered and signed in.
 * - is_admin: A flag indicating if the user has permission to access
 *   administration layout.
 *
 * Site identity:
 * - front_page: The URL of the front page. Use this instead of base_path when
 *   linking to the front page. This includes the language domain or prefix.
 *
 * Page content (in order of occurrence in the default page.html.twig):
 * - messages: Status and error messages. Should be displayed prominently.
 * - node: Fully loaded node, if there is an automatically-loaded node
 *   associated with the page and the node ID is the second argument in the
 *   page's path (e.g. node/12345 and node/12345/revisions, but not
 *   comment/reply/12345).
 *
 * Regions:
 * - page.header: Items for the header region.
 * - page.primary_menu: Items for the primary menu region.
 * - page.secondary_menu: Items for the secondary menu region.
 * - page.highlighted: Items for the highlighted content region.
 * - page.help: Dynamic help text, mostly for admin layout.
 * - page.content: The main content of the current page.
 * - page.sidebar_first: Items for the first sidebar.
 * - page.sidebar_second: Items for the second sidebar.
 * - page.footer: Items for the footer region.
 * - page.breadcrumb: Items for the breadcrumb region.
 *
 * @see template_preprocess_page()
 * @see html.html.twig
 *
 * @ingroup themeable
 */
#}
<!-- BEGIN content -->
<div class="mainwrap">

  {# Include notice text block twig template #}
  {% include '@day_theme/includes/components/notice-text-block.html.twig' %}

  {# Include header twig template #}
  {% include '@day_theme/includes/header.html.twig' %}

  {#  {{ page.breadcrumb }} #}

  {{ page.highlighted }}

  {{ page.help }}

  <!-- class searchResultPage and categoryPage should be on this main page  -->
  <main class="main searchResultPage categoryArticle _rel-article">
    <!-- Overlay for modal -->
    <div class="popup__overlay"></div>
    <div class="article-grouped-wrap-3">
      <div class="wrapper article-grouped-3">

        {# Include hot topics tags twig template #}
        {% include '@day_theme/includes/components/hot-topics-tags.html.twig' %}

        <div class="grid-row">
          {{ page.breadcrumb }}
        </div>

        <div class="main__box">
          <div class="main__content">
            <div class="infinite-scroll-feed">
              <div class="categoryArticle__box" {% if next_node_id %}data-next-node-id="{{ next_node_id }}"{% endif %}>

                {{ page.content }}

              </div>
            </div>

            {# Include loader ellips twig template #}
            {% include '@day_theme/includes/components/loader-ellips.html.twig' %}

            {% if next_node_url %}
              <div class="infinite-next-node-url" data-infinite-next-node-url="{{ next_node_url }}"></div>
            {% endif %}

            <!-- Pagination -->
            {% if next_node_id %}
              <div class="load-more-news-wrapper">
                <button class="btn__more btn__more-big load-more-news">{{ 'Load more news'|t }}</button>
              </div>
            {% endif %}

          </div>

          <aside class="sidebar _mob-hidden">

            {# Remove / replace #}
            {# Include sidebar 300px ad twig template #}
            {% include '@day_theme/includes/components/temp/sidebar-300px-ad.html.twig' %}
            {# Remove / replace #}

            {# Include sidebar news tabs short block #}
            {{ drupal_block('day_short_tabs_news_block', wrapper=false) }}

            {# Editors choice block #}
            {{ drupal_block('views_block:editors_choice_block-block_1') }}


            {# Remove / replace #}
            {# Include sidebar long google ad twig template #}
            {% include '@day_theme/includes/components/temp/sidebar-long-google-ad.html.twig' %}
            {# Remove / replace #}

            {# Remove / replace #}
            {# Include partners news twig template #}
            {% include '@day_theme/includes/components/temp/partners-news-sidebar.html.twig' %}
            {# Remove / replace #}

            {# Include video twig template #}
            {% include '@day_theme/includes/blocks/video-sidebar.html.twig' %}

            {# Include "Photo of the day block" twig template #}
            {% include '@day_theme/includes/blocks/photo-of-the-day-block.html.twig' %}

          </aside>

          <aside class="sidebar _mob-visible">

            {# Include poll block twig template #}
            {% include '@day_theme/includes/blocks/poll-block.html.twig' %}

            {# Remove / replace #}
            {# Include sidebar 300px ad twig template #}
            {% include '@day_theme/includes/components/temp/sidebar-300px-ad.html.twig' %}
            {# Remove / replace #}

            {# Include sidebar news tabs full block #}
            {{ drupal_block('day_full_tabs_news_block', wrapper=false) }}

            {# Editors choice block #}
            {{ drupal_block('views_block:editors_choice_block-block_1') }}

          </aside>

        </div>

      </div>
    </div>

    <div class="wrapper article-grouped-3 _mob-hidden">
      {# Remove / replace #}

      {# Include partners news twig template #}
      {% include '@day_theme/includes/components/temp/partners-news.html.twig' %}

      {# Remove / replace #}
    </div>

    <!-- Other sections -->
    <div class="wrapper">
      <div class="main__box">
        <div class="main__content">

          {# Include temporary dark books section twig template #}
          {% include '@day_theme/includes/components/temp-books-section.html.twig' %}

        </div>
      </div>
    </div>

    {# Include subscribe twig template #}
    {% include '@day_theme/includes/components/subscribe.html.twig' %}

    {# Include custom cursor twig template #}
    {% include '@day_theme/includes/components/custom-cursor.html.twig' %}

    {# Include back to top twig template #}
    {% include '@day_theme/includes/components/back-to-top.html.twig' %}

    {# Include search modal twig template #}
    {% include '@day_theme/includes/components/search-modal.html.twig' %}

  </main>

  {# Include footer twig template #}
  {% include '@day_theme/includes/footer.html.twig' %}

</div>
